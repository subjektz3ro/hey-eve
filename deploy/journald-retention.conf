# This host-wide policy bounds journal disk use and retention. Eve records
# operational metadata by default; VOICE_DEBUG=1 also records transcript and
# reply content, so a finite retention window is a privacy control for any
# debugging session that enables it.
[Journal]
SystemMaxUse=200M
MaxRetentionSec=14day
